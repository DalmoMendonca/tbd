<div class="jumbotron">
	<h1>TBD</h1>
	<p>
		<b>Incredible Facebook Quizzes</b>
	</p>
</div>


<div id="user_nav">
  <% if current_user %>
    Signed in as <strong><%= "dude" %></strong>!
    <%= link_to "Sign out", signout_path, id: "sign_out" %>
  <% else %>
    <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %>
  <% end %>
</div>

<div>
	<% if current_user %>
		<div>
  		<table>
  			<tr>
  				<td>
  				Hello <b><%= current_user.name %></b>!
  				</td>
  			</tr>
  		</table>
		</div>
	<% else %>
		<div>
			<%= link_to "Find Backstabbers!", "/auth/facebook", id: "sign_in", class: "btn btn-primary btn-lg" %>
		</div>
	<% end %>
 </div>