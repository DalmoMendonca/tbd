<div class="jumbotron">
	<h1>TBD</h1>
	<p>
		<b>Incredible Facebook Quizzes</b>
	</p>
</div>

<div>
	<% if current_user %>
		<div>
  		<table>
  			<tr>
  				<td>
  				Hello <b><%= current_user.name %></b>! You are the current user.
  				</td>
  			</tr>
  		</table>
		</div>
	<% else %>
		<div id="backstabers">
			<%= link_to "Find Backstabbers!", "/auth/facebook", id: "sign_in", class: "btn btn-primary btn-lg" %>
		</div>
	<% end %>
 </div>